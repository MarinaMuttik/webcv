development:
  adapter: postgresql
  host: <%= ENV['PGHOST'] %>
  username: <%= ENV['PGUSER'] %>
  database: webcv_development
  encoding: utf8

test:
  adapter: postgresql
  host: <%= ENV['PGHOST'] %>
  username: <%= ENV['PGUSER'] %>
  database: webcv_development
  encoding: utf8
